## 1. 架构设计

```mermaid
graph TB
    subgraph "前端展示层"
        A1["React 18 SPA"]
        A2["React Router 路由管理"]
        A3["TailwindCSS 样式系统"]
        A4["ECharts 数据可视化"]
        A5["Lucide React 图标库"]
    end

    subgraph "状态与数据层"
        B1["React useState/useReducer"]
        B2["Mock 数据服务层"]
        B3["本地存储 (localStorage)"]
    end

    subgraph "组件层"
        C1["布局组件 (Layout/Sidebar/Header)"]
        C2["通用组件 (StatCard/Table/Modal)"]
        C3["图表组件 (LineChart/BarChart/PieChart/Gauge)"]
        C4["地图组件 (WellMap/SettlementHeatmap)"]
        C5["表单组件 (FilterPanel/RechargeForm)"]
    end

    subgraph "页面层"
        D1["监测井网页面"]
        D2["水位数据页面"]
        D3["降深趋势页面"]
        D4["地面沉降页面"]
        D5["超采预警页面"]
        D6["回灌管理页面"]
        D7["报告生成页面"]
    end

    A1 --> A2
    A1 --> A3
    A1 --> A4
    A1 --> A5
    A1 --> B1
    B1 --> B2
    A1 --> C1
    A1 --> C2
    A1 --> C3
    A1 --> C4
    A1 --> C5
    C1 --> D1
    C1 --> D2
    C1 --> D3
    C1 --> D4
    C1 --> D5
    C1 --> D6
    C1 --> D7
    D1 --> C3
    D1 --> C4
    D2 --> C2
    D2 --> C3
    D3 --> C3
    D4 --> C3
    D4 --> C4
    D5 --> C2
    D5 --> C4
    D6 --> C2
    D6 --> C5
    D7 --> C2
    D7 --> C3
```

## 2. 技术描述

- **前端框架**: React@18 + TypeScript@5，函数式组件 + Hooks 开发模式
- **构建工具**: Vite@5，快速开发热更新，优化打包构建
- **路由管理**: React Router DOM@6，SPA 单页应用路由
- **样式方案**: TailwindCSS@3，原子化CSS，PostCSS 预处理
- **图表库**: ECharts@5 + echarts-for-react，专业数据可视化
- **图标库**: Lucide React@0.400，线性图标体系
- **日期处理**: date-fns@3，轻量级日期工具库
- **模拟数据**: 纯前端 Mock 数据，内置在 data/ 目录下的 TypeScript 文件中
- **代码规范**: ESLint + Prettier，统一代码风格

## 3. 路由定义

| 路由路径 | 页面名称 | 页面说明 |
|----------|----------|----------|
| / | 监测井网 | 首页，GIS分布地图、统计概览、井点列表 |
| /water-level | 水位数据 | 实时水位采集、数据表格、水质监测、数据质控、开采量统计 |
| /drawdown-trend | 降深趋势 | 水位降深趋势图、多井对比、季节性分析、年度趋势 |
| /land-subsidence | 地面沉降 | 沉降关联分析、沉降分布、耦合曲线、风险评估 |
| /overdraft-warning | 超采预警 | 超采区分布、预警看板、事件列表、处置流程 |
| /recharge-management | 回灌管理 | 回灌登记、回灌井管理、回灌量统计、效果评估 |
| /report-generation | 报告生成 | 年度通报、历史查询、自定义报表、导出功能 |

## 4. 数据模型

### 4.1 数据模型定义 (ER图)

```mermaid
erDiagram
    MONITORING_WELL ||--o{ WATER_LEVEL_RECORD : has
    MONITORING_WELL ||--o{ WATER_QUALITY_RECORD : has
    MONITORING_WELL ||--o{ MAINTENANCE_RECORD : has
    MONITORING_WELL ||--o{ RECHARGE_RECORD : "recharged at"
    MONITORING_WELL ||--o{ WARNING_EVENT : triggers
    SUBSIDENCE_STATION ||--o{ SUBSIDENCE_RECORD : has
    RECHARGE_RECORD ||--|| RECHARGE_WELL : "via"

    MONITORING_WELL {
        string well_id PK "井编号"
        string well_name "井名称"
        string district "所属区域"
        float longitude "经度"
        float latitude "纬度"
        float well_depth "井深(m)"
        float water_level_baseline "基准水位(m)"
        string aquifer_type "含水层类型"
        string status "状态(在线/离线/维护)"
        date establish_date "建站日期"
        string device_model "设备型号"
    }

    WATER_LEVEL_RECORD {
        string record_id PK "记录ID"
        string well_id FK "井编号"
        datetime collect_time "采集时间"
        float water_level "水位埋深(m)"
        float water_temperature "水温(℃)"
        float conductivity "电导率(μS/cm)"
        boolean is_anomaly "是否异常"
        string quality_flag "质控标记"
    }

    WATER_QUALITY_RECORD {
        string record_id PK "记录ID"
        string well_id FK "井编号"
        datetime test_date "检测日期"
        float ph_value "pH值"
        float tds "溶解性总固体(mg/L)"
        float total_hardness "总硬度(mg/L)"
        float fluoride "氟化物(mg/L)"
        string quality_grade "水质类别"
    }

    SUBSIDENCE_STATION {
        string station_id PK "监测点编号"
        string station_name "监测点名称"
        float longitude "经度"
        float latitude "纬度"
        float initial_elevation "初始高程(m)"
        string related_wells "关联井点"
    }

    SUBSIDENCE_RECORD {
        string record_id PK "记录ID"
        string station_id FK "监测点编号"
        date measure_date "测量日期"
        float elevation "当前高程(m)"
        float cumulative_settlement "累计沉降量(mm)"
        float annual_settlement "年沉降量(mm)"
    }

    WARNING_EVENT {
        string warning_id PK "预警ID"
        string well_id FK "触发井点"
        datetime trigger_time "触发时间"
        string warning_level "预警等级(红/橙/黄/蓝)"
        string warning_type "预警类型"
        float trigger_value "触发数值"
        float threshold "阈值"
        string status "状态(待处置/处理中/已解决)"
        string handler "责任人"
        text description "预警描述"
    }

    RECHARGE_WELL {
        string well_id PK "回灌井编号"
        string well_name "回灌井名称"
        string district "所属区域"
        float design_capacity "设计回灌量(m³/d)"
        string recharge_source "回灌水源"
        string status "状态"
    }

    RECHARGE_RECORD {
        string record_id PK "记录ID"
        string well_id FK "回灌井编号"
        date recharge_date "回灌日期"
        float recharge_volume "回灌水量(m³)"
        float recharge_hours "回灌时长(h)"
        string operator "操作人员"
        string water_source "水源类型"
        text remarks "备注"
    }

    MAINTENANCE_RECORD {
        string record_id PK "记录ID"
        string well_id FK "井编号"
        date maintenance_date "维护日期"
        string maintenance_type "维护类型"
        string content "维护内容"
        string operator "维护人员"
        text result "维护结果"
    }
```

### 4.2 模拟数据定义

所有数据使用 TypeScript 常量定义，存储在 `src/data/` 目录下：

- `wells.ts` - 监测井基础数据（50+井点）
- `waterLevel.ts` - 水位采集记录（按天生成，覆盖近3年）
- `waterQuality.ts` - 水质检测记录
- `subsidence.ts` - 沉降监测数据
- `warnings.ts` - 预警事件记录
- `recharge.ts` - 回灌管理数据
- `maintenance.ts` - 设备维护记录

数据生成原则：
- 井点按行政区划均匀分布，包含不同类型（深层承压井、浅层井、观测井）
- 水位数据包含季节性波动、逐年下降趋势、异常突变点
- 预警数据覆盖红/橙/黄/蓝四级，状态分布合理
- 沉降数据与水位降深呈现正相关性
